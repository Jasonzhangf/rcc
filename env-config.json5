{
  "database": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "credentials": {
      "username": "${DB_USER}",
      "password": "${DB_PASSWORD}"
    }
  },
  "environment": "${NODE_ENV}"
}